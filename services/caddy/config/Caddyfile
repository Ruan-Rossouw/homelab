# Local-only TLS termination for .home services, using a private CA (mkcert)
# trusted only on this household's own devices -- not a publicly-trusted
# certificate, and not meant to be. See services/caddy/README.md for why.
#
# Every block below points at the target service's LAN IP:port, per the
# established cross-container rule in this repo (services are independent
# compose projects with no shared Docker network, so containers reach each
# other via the host's LAN IP, never a container name or localhost).
#
# One shared cert (home-services.pem/-key.pem) covers every hostname below --
# NOT a wildcard (*.home was tried first and rejected in practice by curl/
# Safari/Firefox: a wildcard directly under a single-label base domain like
# `.home` is treated the same as `*.com`, too broad to trust). Adding a new
# service means regenerating that cert with the full hostname list, not just
# adding a Caddyfile block -- see the README's "Adding Another Service"
# section for the exact command.
#
# Hostnames match this repo's existing AdGuard DNS rewrites (runtime config,
# not tracked here) as of 2026-08-01 -- see services/adguard/README.md.

adguard.home {
	tls /certs/home-services.pem /certs/home-services-key.pem
	reverse_proxy 192.168.68.110:3000
}

# ZimaOS's own dashboard, not one of this repo's tracked services -- port 80
# per the existing "Known Gotcha" note in services/adguard/README.md.
server.home {
	tls /certs/home-services.pem /certs/home-services-key.pem
	reverse_proxy 192.168.68.110:80
}

# Portainer serves HTTPS itself (self-signed, port 9443) -- Caddy connects
# to it over HTTPS too and skips upstream cert verification, since that
# leg never leaves the host and the self-signed cert is Portainer's own
# known, expected default, not a spoofing risk to guard against here.
portainer.home {
	tls /certs/home-services.pem /certs/home-services-key.pem
	reverse_proxy https://192.168.68.110:9443 {
		transport http {
			tls_insecure_skip_verify
		}
	}
}

uptime.home {
	tls /certs/home-services.pem /certs/home-services-key.pem
	reverse_proxy 192.168.68.110:3001
}

grafana.home {
	tls /certs/home-services.pem /certs/home-services-key.pem
	reverse_proxy 192.168.68.110:3002
}

backrest.home {
	tls /certs/home-services.pem /certs/home-services-key.pem
	reverse_proxy 192.168.68.110:9898
}

jellyfin.home {
	tls /certs/home-services.pem /certs/home-services-key.pem
	reverse_proxy 192.168.68.110:8096
}

decypharr.home {
	tls /certs/home-services.pem /certs/home-services-key.pem
	reverse_proxy 192.168.68.110:8282
}

prowlarr.home {
	tls /certs/home-services.pem /certs/home-services-key.pem
	reverse_proxy 192.168.68.110:9696
}

radarr.home {
	tls /certs/home-services.pem /certs/home-services-key.pem
	reverse_proxy 192.168.68.110:7878
}

sonarr.home {
	tls /certs/home-services.pem /certs/home-services-key.pem
	reverse_proxy 192.168.68.110:8989
}

seerr.home {
	tls /certs/home-services.pem /certs/home-services-key.pem
	reverse_proxy 192.168.68.110:5055
}

home-assistant.home {
	tls /certs/home-services.pem /certs/home-services-key.pem
	reverse_proxy 192.168.68.110:8123
}
