# Copy to .env and adjust if needed.

# Host port for the Backrest web UI.
BACKREST_PORT=9898

# Timezone Backrest uses for its own schedule display/logging. Inferred
# from "BST" timestamps already seen in this server's systemd/restic
# output (Europe/London) — correct this if that's wrong.
TZ=Europe/London
