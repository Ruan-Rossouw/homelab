# Copy to .env and adjust if needed. Not required for a default install.
#
# node-exporter uses network_mode: host, so there's no host port to
# override here — it always listens on the host's port 9100 directly.
