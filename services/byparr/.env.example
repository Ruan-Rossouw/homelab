# Copy to .env and adjust if needed.

# Host port for Byparr's API. Deliberately not 8191 (FlareSolverr's port)
# so both can run side by side during the migration's proving phase --
# see README.md.
BYPARR_PORT=8192

# Log level: debug, info, warning, error.
LOG_LEVEL=info
